import { Permanent_Marker, Open_Sans } from "next/font/google";

export const permanent_marker = Permanent_Marker({
    weight: ["400"],
    subsets: ["latin"]
})

export const open_sans = Open_Sans({
    weight: ["400"],
    subsets: ["latin"]
})